<view class="card" bindtap="open"><image class="image" src="{{poi.cover}}" mode="aspectFill" binderror="imageError" wx:if="{{poi.cover && !failed}}"/><view class="image fallback" wx:else>{{poi.category}}</view><view class="body"><view class="top"><text class="tag">{{poi.category}}</text></view><view class="name">{{poi.name}}</view><view class="meta">{{poi.subcategory}}</view><view class="reason">{{poi.description}}</view></view></view>
