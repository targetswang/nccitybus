<view class="safe-page"><top-header title="沿线漫游" subtitle="路线玩法 · 沿线地点"/><view class="page-content"><view class="intro-title">像当地人一样，<br/>慢慢认识南充。</view><view class="intro-sub">路线告诉你一段时间怎么玩，地点告诉你这一站值得去哪。</view><view wx:if="{{error}}" class="error">{{error}}</view><view class="switch"><view class="{{mode==='walks'?'active':''}}" data-mode="walks" bindtap="switchMode">路线玩法</view><view class="{{mode==='places'?'active':''}}" data-mode="places" bindtap="switchMode">沿线地点</view></view><view wx:if="{{mode==='walks'}}"><walk-card wx:for="{{walks}}" wx:key="id" walk="{{item}}"/></view><view wx:else><scroll-view scroll-x class="filters"><view wx:for="{{categories}}" wx:key="*this" class="{{category===item?'active':''}}" data-cat="{{item}}" bindtap="filter">{{item}}</view></scroll-view><picker mode="selector" range="{{stationOptions}}" range-key="name" value="0" bindchange="station"><view class="station-picker">按站点筛选：{{nodeId==='all'?'全部站点':'已选择站点'}}</view></picker><poi-card wx:for="{{pois}}" wx:key="id" poi="{{item}}"/><view wx:if="{{!pois.length}}" class="empty">当前条件下没有地点。</view></view></view><bottom-nav active="explore"/></view>
