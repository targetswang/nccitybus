<view class="safe-page"><top-header title="全部环线站点" subtitle="城市漫游环线" showBack="{{true}}"/><view class="page-content"><view wx:if="{{error}}" class="error">{{error}}</view><view wx:for="{{stations}}" wx:key="id" class="station" data-id="{{item.id}}" bindtap="open"><view class="dot">{{index+1}}</view><view><view class="name">{{item.name}}</view><view class="muted">{{item.persona}}</view></view></view></view><bottom-nav active="home"/></view>
