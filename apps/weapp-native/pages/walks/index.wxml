<view class="safe-page"><top-header title="城市玩法" subtitle="像当地人一样玩南充" showBack="{{true}}"/><view class="page-content"><view wx:if="{{error}}" class="empty">{{error}}</view><walk-card wx:for="{{walks}}" wx:key="id" walk="{{item}}"/></view><bottom-nav active="explore"/></view>
