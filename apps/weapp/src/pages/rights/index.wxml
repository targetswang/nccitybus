<view class="screen">
<view wx:if="{{error}}" class="error-box">
<text>{{error}}</text>
<button bindtap="retry">重新加载</button>
</view>
<view wx:if="{{loading}}" class="loading">正在读取…</view>
<view class="content">
<view class="h1">我的权益</view>
<view class="panel">
<view class="h2">暂无已发布活动</view>
<view class="copy">这里仅显示已开放的游客活动，不影响日常乘车与漫游服务。</view>
</view>
</view>
<bottom-nav active="{{active || 'home'}}"/>
</view>
