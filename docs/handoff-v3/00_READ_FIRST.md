# 南充嘉陵江城市漫游｜研发接手必读 v3

当前在线联调环境：

- 游客 H5：https://app-3hu1sz.v2.appdeploy.ai/
- 运营后台：https://app-sz13s5.v2.appdeploy.ai/

系统采用一套业务数据、两个游客客户端（H5 / 微信原生小程序）、一个运营后台。

当前均属于 integration-preview。未配置的短信、微信手机号、地图、IVY 实时公交、乘车码不会返回模拟成功。

## 联调管理员

后台使用已授权管理员手机号登录。当前首位管理员以脱敏形式展示为 `188****2117`。

联调测试验证码：`246810`

该验证码只用于隔离联调，不发送短信、不证明手机号所有权，生产必须删除固定验证码机制。

## 数据原则

1. 已发布内容由 `/api/v1/content` 唯一提供。
2. 账户事实由 `/api/v1/me/*` 提供。
3. 实时公交由 `/api/v1/transit/live` 提供。
4. 运营后台保存草稿不会立即影响游客端；必须审核并发布。
5. H5 与微信小程序不得维护第二套 POI / City Walk / 会员 / 活动数据。
